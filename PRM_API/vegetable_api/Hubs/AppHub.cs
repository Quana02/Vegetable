using Microsoft.AspNetCore.SignalR;

namespace vegetable_api.Hubs;

public class AppHub : Hub
{
    // Hub class to handle SignalR connections
    // You can add specific methods here if the client needs to call server methods directly
}
